import { chromium } from 'playwright';import fs from 'node:fs';import path from 'node:path';const out=path.resolve('../../generated/rhine/final_placeholder/frames');fs.mkdirSync(out,{recursive:true});const browser=await chromium.launch({headless:true});const page=await browser.newPage({viewport:{width:1920,height:1080}});for(let frame=0;frame<1008;frame++){await page.goto(`http://127.0.0.1:5173/?frame=${frame}`);await page.screenshot({path:path.join(out,`${String(frame).padStart(6,'0')}.png`)})}await browser.close();
